# Activity Diagrams

**Sistem:** BAPENDA.HUB — Portal Pajak Daerah Kota Medan

---

## 1. Registrasi & Login

```
[Pengunjung]              [Sistem]                 [Database]
     │                        │                        │
     │── Buka Login/Reg ────→│                        │
     │                        │── Tampilkan Form ────→│
     │←── Form ──────────────│                        │
     │                        │                        │
     │── Isi Data ──────────→│                        │
     │                        │── Validasi ──────────→│
     │                        │     ├─ Valid ────────→│── Simpan User
     │                        │     │                 │── Create Session
     │←── Sukses + Token ─────│     │                 │
     │                        │     └─ Invalid ──────→│
     │←── Error ──────────────│                        │
```

## 2. Pembayaran Pajak via Midtrans

```
[WP]                  [Sistem BE]           [Midtrans]          [Database]
 │                        │                      │                  │
 │── Pilih Tagihan ──────→│                      │                  │
 │                        │── Cek Tagihan ───────│─────────────────→│
 │                        │←── Data Tagihan ─────│──────────────────│
 │                        │                      │                  │
 │                        │── Init Payment ─────→│                  │
 │                        │←── Payment URL ──────│                  │
 │←── Redirect URL ───────│                      │                  │
 │                        │                      │                  │
 │── Bayar di Midtrans ─────────────────────────→│                  │
 │←── Selesai ───────────────────────────────────│                  │
 │                        │                      │                  │
 │                        │←── Webhook Callback ─│                  │
 │                        │── Update Status ─────│─────────────────→│
 │                        │── Kirim Notif ───────│─────────────────→│
 │←── Notifikasi ─────────│                      │                  │
```

## 3. Proses Pengajuan (Keberatan/Perubahan)

```
[WP]                  [Sistem]                [Admin]             [Database]
 │                        │                      │                  │
 │── Buat Pengajuan ─────→│                      │                  │
 │── Upload Dokumen ─────→│                      │                  │
 │                        │── Simpan PENDING ────│─────────────────→│
 │←── Tiket Diterbitkan ──│                      │                  │
 │                        │── Notif Admin ──────→│                  │
 │                        │                      │                  │
 │                        │                      │── Review ───────→│
 │                        │                      │── Update Status ─│
 │                        │←── Hasil Review ─────│                  │
 │                        │                      │                  │
 │←── Notif Status ───────│                      │                  │
 │                        │                      │                  │
 │── Cek Status ─────────→│                      │                  │
 │                        │── Query Status ──────│─────────────────→│
 │←── Status + Notes ─────│                      │                  │
```

## 4. Chatbot Interaction

```
[User]                [Chatbot UI]          [API route.ts]       [Database]
 │                        │                      │                  │
 │── Klik Floating ──────→│                      │                  │
 │                        │── Buka Panel ────────│                  │
 │←── Sapaan + Chips ─────│                      │                  │
 │                        │                      │                  │
 │── Ketik/Click Chip ───→│                      │                  │
 │                        │── POST /api/chatbot ─→│                  │
 │                        │                      │── Validasi Input  │
 │                        │                      │── Rate Limit Cek  │
 │                        │                      │── Spam Detection  │
 │                        │                      │                  │
 │                        │                      │── Intent Match ──→│
 │                        │                      │     ├ info umum   │── Query static
 │                        │                      │     ├ data pribadi│── Query DB (jika login)
 │                        │                      │     └ tindakan   │── Gate auth + DB
 │                        │                      │                  │
 │                        │←── Response ─────────│                  │
 │←── Tampilkan Chat ─────│                      │                  │
 │                        │                      │                  │
 │                        │── Save History ──────│─────────────────→│
```

## 5. Pendataan Lapangan oleh Petugas

```
[Petugas]               [Sistem]              [Database]         [Admin]
 │                        │                      │                  │
 │── Login OFFICER ──────→│                      │                  │
 │←── Dashboard ──────────│                      │                  │
 │                        │                      │                  │
 │── Pilih Pendataan ────→│                      │                  │
 │── Cari Objek Pajak ───→│                      │                  │
 │                        │── Query Objek ───────│─────────────────→│
 │←── Data Objek ─────────│                      │                  │
 │                        │                      │                  │
 │── Update Data ────────→│                      │                  │
 │── Tandai Lokasi ──────→│                      │                  │
 │── Upload Foto ────────→│                      │                  │
 │                        │── Simpan Perubahan ──│─────────────────→│
 │                        │── Catat Audit Log ───│─────────────────→│
 │←── Sukses ─────────────│                      │                  │
 │                        │── Kirim Notif ──────→│─────────────────→│── Review
```

## 6. Proses PPID (Informasi Publik)

```
[Pemohon]               [Sistem]              [Admin PPID]       [Database]
 │                        │                      │                  │
 │── Ajukan PPID ───────→│                      │                  │
 │── Isi Form ───────────→│                      │                  │
 │                        │── Simpan ────────────│─────────────────→│
 │←── Tiket Terbit ───────│                      │                  │
 │                        │── Notif Admin ──────→│                  │
 │                        │                      │                  │
 │                        │                      │── Review ───────→│
 │                        │                      │── Siapkan Jawaban │
 │                        │                      │── Upload Dokumen ─│
 │                        │←── Jawaban ──────────│                  │
 │                        │── Update Status ─────│─────────────────→│
 │←── Notif Selesai ──────│                      │                  │
 │                        │                      │                  │
 │── Lihat Jawaban ──────→│                      │                  │
 │                        │── Tampilkan Data ────│─────────────────→│
```
